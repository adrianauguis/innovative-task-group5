class PassArguments {
  String name, imgAsset, relShip, occPat, bDay, age, pg2bg;

  PassArguments(
      {required this.name,
      required this.imgAsset,
      required this.relShip,
      required this.occPat,
      required this.bDay,
      required this.age,
      required this.pg2bg});
}
